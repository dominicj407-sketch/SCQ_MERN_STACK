{
  "name": "smartcarequeue-deploy",
  "private": true,
  "scripts": {
    "render-build": "npm ci --prefix backend --include=dev && npm ci --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}
